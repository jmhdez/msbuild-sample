Sample compilation script using MSBuild.

The script performs the following steps:

1. Build the solution
2. Run unit tests
3. Create a nuget package

Just run `build.cmd`. The results will be stored in `.\results\` folder.

